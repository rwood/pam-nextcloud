# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must use LF line endings
*.sh text eol=lf
*.bash text eol=lf

# Python scripts must use LF line endings
*.py text eol=lf

# Configuration files
*.conf text eol=lf
*.ini text eol=lf

# Markdown files
*.md text eol=lf

# Batch files for Windows (these should use CRLF)
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

